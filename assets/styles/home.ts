import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  someClass: {
    flex: 1,  
  },

  image: {
    // borderColor: 'white',
    // borderWidth: 1,
    opacity: 0,
    width: '100%',
    height: '100%',
  },
});